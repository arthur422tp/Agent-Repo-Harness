# CLAUDE.md

Read this file first in Claude Code, then use `.claude/skills` if installed.
Follow `agent.md`, `handoff.md`, `.agent/policy.yml`, and `.agent/task.yml`.
Run `scripts/agent-finish.sh` before final response.
