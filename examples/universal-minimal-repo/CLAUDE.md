# CLAUDE.md

Read this file first in Claude Code, then use `.claude/skills` if installed.
Follow `agent.md` and `handoff.md`. Read `.agent/task.yml` for scope and
completion requirements, then read only applicable `.agent/policy.yml` entries
for files you expect to touch. Run `scripts/agent-finish.sh` before final
response.
