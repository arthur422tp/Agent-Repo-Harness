# Scripts

In a real target repository these scripts are installed from
`templates/scripts/`.

This example includes tiny working scripts so the entrypoints, `.agent/`
state, and finish evidence flow can be inspected without installing the full
template payload.
