{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://agent-repo-harness.local/schemas/review.schema.json",
  "title": "Agent-Repo-Harness review evidence",
  "type": "object",
  "required": ["review"],
  "properties": {
    "review": {
      "type": "object",
      "required": ["required", "status", "reviewer", "evidence", "concerns"],
      "properties": {
        "required": { "const": true },
        "status": {
          "enum": [
            "approved",
            "approved_with_comments"
          ]
        },
        "reviewer": { "type": "string", "pattern": "\\S" },
        "evidence": { "type": "string", "pattern": "\\S" },
        "concerns": {
          "type": "array",
          "items": {
            "oneOf": [
              { "type": "string" },
              {
                "type": "object",
                "properties": {
                  "id": { "type": "string" },
                  "description": { "type": "string" },
                  "blocking": { "type": "boolean" }
                },
                "additionalProperties": true,
                "not": {
                  "required": ["blocking"],
                  "properties": {
                    "blocking": { "const": true }
                  }
                }
              }
            ]
          }
        }
      },
      "additionalProperties": true
    }
  },
  "additionalProperties": true
}
